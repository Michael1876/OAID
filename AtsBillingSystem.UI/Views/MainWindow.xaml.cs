using System.Windows;

// ВАЖНО: Пространство имен должно ТОЧНО совпадать с папкой и атрибутом x:Class в XAML
namespace AtsBillingSystem.UI.Views
{
    /// <summary>
    /// Interaction logic for MainWindow.xaml
    /// </summary>
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            InitializeComponent();
        }
    }
}